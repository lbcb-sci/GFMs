[project]
name = "gfms"
dynamic = ["version"]
description = "Critical analysis of Genomic Foundation Models"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ckatorch>=1.0.2",
    "datasets>=4.5.0",
    "numpy>=2.2.6",
    "scipy>=1.15.3",
    "tokenizers>=0.22.2",
    "torch>=2.10.0",
    "transformers>=5.0.0",
]
