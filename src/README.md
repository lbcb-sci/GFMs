### Description 
- `embeddings`: code to compute embedddings and save them on disk for reuse 
- `distance`: code to compute distance matrices and save them on disk for reuse 
- `correlation`: code to compute correlation between distance matrices  
- `clustering`:  code to plot clustering using umap  
- `common`: constants and utils
- `models`: wrappers around models
- `datasets`: wrappers around datasets
